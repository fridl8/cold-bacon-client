import { StyleSheet, Dimensions } from 'react-native';

export default StyleSheet.create({
  touchableSize: {
    width: 60,
    alignItems: 'center',
    // borderWidth: 1,
  },
  image: {
    height: 65,
    width: 65,
    borderRadius: 32,
  }
});
